apiVersion: v1
kind: Service
metadata:
  name: data-preprocessing-service
spec:
  selector:
    app: data-preprocessing
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP